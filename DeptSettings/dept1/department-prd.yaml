targetArgoName: gitops-prd-dept1
targetArgoProject: default
targetArgoManagedNamespaces:
  - prd-app1-namespace
  - prd-app2-namespace
targetArgoRBACPolicy: |
      g, system:cluster-admins, role:admin
      g, cluster-admins, role:admin
      g, gitops-admins, role:admin
